package clustering.algorithm;

import java.util.ArrayList;

/**
 * Hierarchical
 */
public class Hierarchical extends Clustering {
    
    public Hierarchical(ArrayList< Datapoint > points, ArrayList< Cluster > clusters) {
        super(points, clusters);
    }

}